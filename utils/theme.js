import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#144",
    },
    secondary: {
      main: "#199",
    },
    background: {
      default: "#ffffff",
    },
  },
});

export default theme;
